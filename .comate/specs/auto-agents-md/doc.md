# 实现 AGENTS.md 自动生成和增量更新功能

## 背景与问题

当前 `harness-apply` 在 **Reentry Mode** 时不会更新 `AGENTS.md`，因为 `generate.js` 中的 `incremental` 类型未实现。具体表现：

- **Initial Mode** (无 manifest.json) → ✓ 生成 AGENTS.md
- **Reentry Mode** (有 manifest.json) → ✗ 不更新 AGENTS.md

代码位置：`plugins/harness-pilot/skills/harness-apply/tools/generate.js` 第 278-284 行

```javascript
if (type === 'harness') {
  result = generateHarness(options);
} else if (type === 'code') {
  result = generateCode(options);
} else {
  result = { success: false, error: `Unknown type: ${type}` };  // ← incremental 走到这里报错
}
```

## 需求描述

实现 `generateIncremental` 函数，支持 AGENTS.md 的智能更新：

| 项目状态 | 行为 |
|----------|------|
| 有 manifest.json（harness-pilot 创建） | 默认更新 AGENTS.md |
| 无 manifest.json（非 harness-pilot 创建） | 询问用户是否要生成/补充 |

## 技术方案

### 1. 实现 generateIncremental 函数

```javascript
function generateIncremental(options) {
  const { language, framework, projectDir = process.cwd() } = options;

  // 检查 manifest.json 是否存在
  const manifestPath = getManifestPath(projectDir);
  const hasManifest = fs.existsSync(manifestPath);

  // 如果是 harness-pilot 创建的项目，直接更新 AGENTS.md
  if (hasManifest) {
    const context = {
      PROJECT_NAME: path.basename(projectDir),
      LANGUAGE: language,
      FRAMEWORK: framework,
      GENERATED_DATE: new Date().toISOString().split('T')[0],
      CURRENT_YEAR: new Date().getFullYear().toString()
    };

    const engine = new TemplateEngine();
    engine.setContext(context);

    const templatePath = resolveTemplate('AGENTS', language, framework);
    if (templatePath && fs.existsSync(templatePath)) {
      const template = fs.readFileSync(templatePath, 'utf8');
      const rendered = engine.render(template);
      const outputPath = path.join(projectDir, 'AGENTS.md');

      // 更新 manifest 的 lastApplied
      const manifest = JSON.parse(fs.readFileSync(manifestPath, 'utf8'));
      manifest.lastApplied = new Date().toISOString();
      fs.writeFileSync(manifestPath, JSON.stringify(manifest, null, 2), 'utf8');

      fs.writeFileSync(outputPath, rendered, 'utf8');
      return { success: true, results: [{ type: 'file', path: 'AGENTS.md', action: 'updated' }] };
    }
  }

  // 如果不是 harness-pilot 创建的项目，返回需要用户确认的状态
  return {
    success: true,
    requiresConfirmation: true,
    message: 'AGENTS.md not managed by harness-pilot. Generate or append?',
    results: []
  };
}
```

### 2. 修改 generate.js CLI 入口

将 `incremental` 加入处理分支：

```javascript
if (type === 'harness') {
  result = generateHarness(options);
} else if (type === 'code') {
  result = generateCode(options);
} else if (type === 'incremental') {
  result = generateIncremental(options);
} else {
  result = { success: false, error: `Unknown type: ${type}` };
}
```

### 3. SKILL.md 流程调整

在 Reentry Mode 工作流中添加 AGENTS.md 更新步骤：

```
1. CALL detect()
2. CALL generate("incremental", { changes })
3. IF requiresConfirmation:
   - 询问用户：是否生成/补充 AGENTS.md？
   - 如果 yes: 调用 generate("harness", { language, framework, components: ['AGENTS'] })
4. CALL loop("run")
```

## 受影响文件

| 文件 | 修改类型 |
|------|----------|
| `plugins/harness-pilot/skills/harness-apply/tools/generate.js` | 新增 `generateIncremental` 函数，修改 CLI 入口 |
| `plugins/harness-pilot/skills/harness-apply/SKILL.md` | 更新 Reentry Mode 工作流 |

## 不在本次范围

- 修改 AGENTS.md 模板内容
- 修改其他文档（ARCHITECTURE.md、DEVELOPMENT.md 等）的更新逻辑
- meta-project（harness-pilot 自身）的特殊处理（已在之前 spec 中定义）

## 边界情况

1. **AGENTS.md 已被手工修改**：如果文件头部包含 `Auto-generation: disabled` 标记，跳过更新
2. **模板不存在**：返回 warning，不中断流程
3. **manifest.json 损坏**：返回 error，提示用户重新初始化

## 验证标准

- [ ] Initial Mode 正常生成 AGENTS.md
- [ ] Reentry Mode（有 manifest）自动更新 AGENTS.md
- [ ] Reentry Mode（无 manifest）返回 requiresConfirmation
- [ ] 所有测试通过（如 template-engine.test.js）

---
*Generated by harness-apply on 2026-04-28*
# Summary — AGENTS.md 自动生成和增量更新功能

## 背景

harness-apply 的 Reentry Mode 不支持更新 AGENTS.md，因为 `generate.js` 中的 `incremental` 类型未实现。

## 已完成的工作

### 1. 实现 generateIncremental 函数

在 `plugins/harness-pilot/skills/harness-apply/tools/generate.js` 中新增：

```javascript
function generateIncremental(options) {
  // 检查 manifest.json 是否存在
  // 存在：自动更新 AGENTS.md
  // 不存在：返回 requiresConfirmation
}
```

### 2. 修改 CLI 入口

- 添加 `incremental` 类型处理分支（第 338-340 行）
- 导出 `generateIncremental` 函数（第 360 行）

### 3. 更新 SKILL.md

在 Reentry Mode Workflow 中添加：
- 步骤 2 的 AGENTS.md 更新说明
- `requiresConfirmation` 的处理逻辑

### 4. 测试验证

| 测试场景 | 结果 |
|---------|------|
| Initial Mode 生成 AGENTS.md | ✓ 通过 |
| Reentry Mode (有 manifest) 更新 AGENTS.md | ✓ 通过 |
| Reentry Mode (无 manifest) 返回 requiresConfirmation | ✓ 通过 |

## 测试输出示例

**有 manifest 的项目：**
```json
{
  "success": true,
  "results": [
    {
      "type": "file",
      "path": "AGENTS.md",
      "action": "updated"
    }
  ]
}
```

**无 manifest 的项目：**
```json
{
  "success": true,
  "requiresConfirmation": true,
  "message": "AGENTS.md not managed by harness-pilot. Generate or append?",
  "results": []
}
```

## 受影响文件

| 文件 | 修改类型 |
|------|----------|
| `plugins/harness-pilot/skills/harness-apply/tools/generate.js` | 新增 `generateIncremental` 函数，修改 CLI 入口和导出 |
| `plugins/harness-pilot/skills/harness-apply/SKILL.md` | 更新 Reentry Mode 工作流 |
| `.comate/specs/auto-agents-md/doc.md` | 新建 |
| `.comate/specs/auto-agents-md/tasks.md` | 新建 |

## 验证标准

- [x] Initial Mode 正常生成 AGENTS.md
- [x] Reentry Mode（有 manifest）自动更新 AGENTS.md
- [x] Reentry Mode（无 manifest）返回 requiresConfirmation
- [x] 所有测试通过

## 实现日期

2026-04-28

---
*Generated by harness-apply*